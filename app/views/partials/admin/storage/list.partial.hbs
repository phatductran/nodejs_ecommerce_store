<p class="text-muted m-b-30 font-14">Each storage has a unique ID. Management features are added in the 'Action' column.
</p>

<table id="datatable-buttons" class="table table-striped table-bordered dt-responsive nowrap" cellspacing="0"
    width="100%">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Property type</th>
            <th>Capacity</th>
            <th class="text-center">Status</th>
            <th class="text-center">Created at</th>
            <th class="text-center">Action</th>
        </tr>
    </thead>
    <tbody>
        {{#each storages}}
        <tr>
            <td id="{{{this.id}}}">{{{this.id}}}</td>
            <td>{{{this.name}}}</td>
            <td>{{{this.propertyType}}}</td>
            <td>{{{this.capacity}}}</td>
            {{#ifMatch this.status 'available'}}
            <td class="bg-teal text-white capitalized text-center">
            {{/ifMatch}}
            {{#ifMatch this.status 'full'}}
            <td class="bg-danger text-white capitalized text-center">
            {{/ifMatch}}
                {{{this.status}}}
            </td>
            <td class="text-center">{{#formatDate this.createdAt}} {{/formatDate}}</td>
            <td class="text-center">
                <div class="btn-group">
                    <button class="btn btn-secondary" type="button" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        <i class="ion-more font-18"></i>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a class="dropdown-item" href="/admin/storages/view/{{{this.id}}}">
                            <i class="ion-ios7-eye mr-2"></i> View
                        </a>
                        <a class="dropdown-item" href="/admin/storages/edit/{{{this.id}}}" >
                            <i class="ion-edit mr-2"></i> Edit
                        </a>
                        {{#ifeq status 'available'}}
                        <a class="dropdown-item sa-set-full" href="javascript:void(0);" data-uid="{{{this.id}}}">
                            <i class="ion-minus-round mr-2"></i> Set full
                        {{else}}
                        <a class="dropdown-item sa-set-available" href="javascript:void(0);" data-uid="{{{this.id}}}">
                            <i class="ion-checkmark-round mr-2"></i> Set available
                        {{/ifeq}} 
                        </a>
                        <a class="dropdown-item sa-delete" href="javascript:void(0);" data-uid="{{{this.id}}}">
                            <i class="ion-trash-a mr-2"></i> Delete
                        </a>
                    </div>
                </div>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>